# ============================================================================
# 水处理加药/曝气回路 PID 控制仿真 —— 第三方依赖清单
# 说明：仅使用免费开源库；下列版本为本机（Windows 10 + Python 3.12）验证通过的组合
# 安装命令：
#     pip install -r requirements.txt
# 或逐个安装：
#     pip install numpy flask pymodbus==3.6.9
# ============================================================================
numpy>=2.0          # 数值计算：仿真数组、统计指标（IAE/超调/调节时间）
flask>=3.0          # Web 看板后端（REST API + 页面渲染）
pymodbus==3.6.9     # Modbus/TCP 从站/主站模拟（本代码按 3.6.x API 编写并实测）
